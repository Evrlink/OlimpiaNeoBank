# Copy to .env.local — never commit real credentials.
# EXPO_PUBLIC_* values are bundled into the mobile app (public client IDs only).

EXPO_PUBLIC_PRIVY_APP_ID=
EXPO_PUBLIC_PRIVY_CLIENT_ID=
EXPO_PUBLIC_API_BASE_URL=
