# Copy to .env.local (or .env) — never commit real secrets.

NODE_ENV=development
PORT=3001

# PostgreSQL — required for migrations and database health check
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/olimpia_dev
